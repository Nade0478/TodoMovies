{% extends 'base.html.twig' %}

{% block title %}Genre{% endblock %}

{% block body %}
    <h1>Genre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ genre.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ genre.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_genre_index') }}">Retour à la liste</a>

    <a href="{{ path('app_genre_edit', {'id': genre.id}) }}">Editer</a>

    {{ include('genre/_delete_form.html.twig') }}
{% endblock %}
