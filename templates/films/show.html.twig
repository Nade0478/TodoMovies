{% extends 'base.html.twig' %}

{% block title %}Films{% endblock %}

{% block body %}
    <h1>Films</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ film.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ film.title }}</td>
            </tr>
            <tr>
                <th>Autor</th>
                <td>{{ film.autor }}</td>
            </tr>
            <tr>
                <th>Genre</th>
                <td>{{ film.genre }}</td>
            </tr>
            <tr>
                <th>Summarize</th>
                <td>{{ film.summarize }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_films_index') }}">Retout à la liste</a>

    <a href="{{ path('app_films_edit', {'id': film.id}) }}">Valide</a>

    {{ include('films/_delete_form.html.twig') }}
{% endblock %}
